// Public API surface — only these three exports are part of the public contract.
// Internal helpers (stellar.ts, verify.ts, issue.ts) are not exported.
export { HumonicsClient } from './client.js';
export { HumonicsError } from './errors.js';
export type { HumonicsErrorCode } from './errors.js';
